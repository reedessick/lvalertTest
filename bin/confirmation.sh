#!/bin/bash

### this script is meant to be run under lvalert_listen in order to confirm that test lvalert messages were sent.
### useful when debugging lvalert_listenMP or other new functionality

read a
echo ${a}
